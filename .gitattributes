# Global normalization
* text=auto

# Force Windows line endings for batch & PowerShell scripts
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf

# Treat common binaries explicitly (add more as needed)
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
